# Legacy Materials

The repository contains historical documentation artifacts in `docs/` (older markdown notes and LaTeX/PDF guide files).

These are retained for archival value, but the Sphinx/RTD pages in this documentation are the maintained source of truth for current configuration and workflow.

Legacy files include:

- `SPIDER_User_Guide.tex` / `SPIDER_User_Guide.pdf`
- `COLLAPSED_GAUSSIAN_SHARED_EVENT_RE.md` (legacy schema terminology)
- `API_REFERENCE.md` and `EXAMPLES.md` (older command/config conventions)
